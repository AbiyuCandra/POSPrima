<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.posprima">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashScreenTheme">
        <activity android:name=".MenuAwal.splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuAwal.login_form"
            android:theme="@style/SplashScreenTheme" />
        <activity
            android:name=".DashboardPackage.Dashboard"
            android:theme="@style/Dashboard" />
        <activity
            android:name=".DashboardPackage.Dashboard_tab"
            android:label="@string/title_activity_dashboard_tab"
            android:theme="@style/SplashScreenTheme" />
        <activity android:name=".DashboardPackage.Dashboard_Library" />
        <activity android:name=".DashboardPackage.Dashboard_Barcode" />
        <activity android:name=".MenuAwal.Welcome_Screen"></activity>
    </application>

</manifest>